{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && surge dist/ vite-page.surge.sh"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
